# OAA v2.0 to v3.0 Conversion - File Catalogue

**What Files Needed for Conversion**  
**Version:** 1.0  
**Date:** 2025-10-13

---

## 🎯 Quick Answer

### Files Needed for Conversion

```
REQUIRED:
✅ Ontology JSON file (v2.0)
   - The core ontology definition
   - This is ALL you need!

OPTIONAL (if you have them):
⚠️ Glossary JSON file
   - Will be REGENERATED by OAA v1.1
   - But can help inform conversion

NOT NEEDED:
❌ Markdown documentation
   - Will be REGENERATED automatically
   - OAA v1.1 creates fresh docs

❌ Test data files
   - Will be REGENERATED automatically
   - OAA v1.1 creates new test data
```

---

## 📋 Conversion Input Strategy

### Option A: Minimal (Recommended) ⭐

```
INPUT:  v2.0 ontology JSON only
PROCESS: OAA v1.1 conversion prompt
OUTPUT: Complete v3.0 with:
        - Ontology definition
        - NEW glossary
        - NEW test data
        - NEW documentation
```

**Why Recommended:**
- Simplest and fastest
- OAA v1.1 regenerates everything fresh
- Ensures consistency with v3.0 standards
- No manual file merging

### Option B: Enhanced (If You Want)

```
INPUT:  v2.0 ontology JSON
        + v2.0 glossary JSON (for context)
PROCESS: OAA v1.1 conversion prompt with note:
         "Use glossary definitions as reference"
OUTPUT: Complete v3.0 with improved definitions
```

**When to Use:**
- Glossary has valuable domain knowledge
- Want to preserve specific terminology
- Definitions are well-crafted

---

## 🗂️ Project Ontology Catalogue

### Ontologies Mentioned in This Project

Based on our conversations, here are the ontologies for BAIV:

---

### 1. **Organization Ontology**

**Purpose:** Define organizational entities and structure

**v2.0 Files (if exist):**
```
org-ontology-v2/
├── ontology.json           ← REQUIRED for conversion
├── glossary.json          ← Optional reference
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `org-ontology-v2/ontology.json` ONLY

**Expected v3.0 Output:**
```
org-ontology-v3.json       ← Complete definition
└── Includes:
    - @context with schema.org
    - Entry ID
    - All entities with schemaOrgBase
    - Relationships
    - Business rules
    - Glossary (regenerated)
    - Test data (regenerated)
    - Metadata
```

---

### 2. **AI Capability Ontology**

**Purpose:** Define AI capabilities, features, and maturity levels

**v2.0 Files (if exist):**
```
ai-capability-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `ai-capability-v2/ontology.json` ONLY

---

### 3. **Market Position Ontology**

**Purpose:** Define market positioning, visibility, competitive landscape

**v2.0 Files (if exist):**
```
market-position-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `market-position-v2/ontology.json` ONLY

---

### 4. **Competitive Analysis Ontology**

**Purpose:** Define competitive analysis framework and metrics

**v2.0 Files (if exist):**
```
competitive-analysis-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `competitive-analysis-v2/ontology.json` ONLY

---

### 5. **AI Maturity Ontology**

**Purpose:** Define AI maturity assessment framework

**v2.0 Files (if exist):**
```
ai-maturity-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `ai-maturity-v2/ontology.json` ONLY

---

### 6. **Visibility Metrics Ontology**

**Purpose:** Define visibility measurement and tracking

**v2.0 Files (if exist):**
```
visibility-metrics-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `visibility-metrics-v2/ontology.json` ONLY

---

### 7. **Strategy Recommendation Ontology**

**Purpose:** Define strategy recommendations and prioritization

**v2.0 Files (if exist):**
```
strategy-recommendation-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `strategy-recommendation-v2/ontology.json` ONLY

---

### 8. **Action Item Ontology**

**Purpose:** Define actionable tasks, timelines, ownership

**v2.0 Files (if exist):**
```
action-item-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `action-item-v2/ontology.json` ONLY

---

### 9. **Timeline Ontology**

**Purpose:** Define temporal structures, milestones, dependencies

**v2.0 Files (if exist):**
```
timeline-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `timeline-v2/ontology.json` ONLY

---

### 10. **Deliverable Ontology**

**Purpose:** Define deliverables, artifacts, acceptance criteria

**v2.0 Files (if exist):**
```
deliverable-v2/
├── ontology.json           ← REQUIRED
├── glossary.json          ← Optional
└── documentation.md       ← Will be regenerated
```

**Conversion Input:**
- `deliverable-v2/ontology.json` ONLY

---

### 11. **Business Process Ontology** (NEW - Create from Scratch)

**Purpose:** Define BAIV Strategy Call process workflow

**v2.0 Files:**
```
N/A - This is NEW for v3.0
```

**Action:**
- Create using BAIV-MVP-WORKFLOW.md Action 4 prompt
- NOT a conversion, but a fresh creation

---

## 📊 Conversion Checklist

### For EACH v2.0 Ontology:

```
☐ Locate v2.0 ontology JSON file
☐ Optional: Locate v2.0 glossary JSON (if you want to reference it)
☐ Prepare conversion prompt (see below)
☐ Run through OAA v1.1
☐ Save output as [name]-v3.json
☐ Add to registry.json
```

---

## 🔄 Conversion Prompt Template

### Basic Conversion (Recommended)

```
Convert this v2.0 ontology to Registry v3.0 format:

[PASTE ONTOLOGY JSON HERE]

Requirements:
1. Add @context with schema.org namespace
2. Generate unique Entry ID (Entry-XXX)
3. Map all entities to schema.org base types
4. Add complete registry metadata:
   - Entry type
   - Domain
   - Date created
   - Tags
   - Dependencies (empty array if none)
5. Calculate quality metrics:
   - Entity count
   - Relationship count
   - Schema.org alignment %
6. Set status: "active"
7. Generate NEW comprehensive glossary
8. Generate NEW test data (5 instances)
9. Add AI/agent capability mappings

Output: Complete v3.0 ontology JSON ready to add to registry
```

### Enhanced Conversion (With Glossary Reference)

```
Convert this v2.0 ontology to Registry v3.0 format:

**v2.0 Ontology:**
[PASTE ONTOLOGY JSON]

**v2.0 Glossary (for reference):**
[PASTE GLOSSARY JSON]

Use the glossary definitions as reference for terminology,
but regenerate in v3.0 format with enhanced structure.

Requirements:
[Same as above]

Output: Complete v3.0 ontology JSON
```

---

## 🗂️ Batch Conversion Strategy

### Option 1: One by One (Safest)

```
Day 1: Convert 3-4 ontologies
├─ Organization
├─ AI Capability
├─ Market Position
└─ Competitive Analysis

Day 2: Convert remaining
├─ AI Maturity
├─ Visibility Metrics
├─ Strategy Recommendation
├─ Action Item
├─ Timeline
└─ Deliverable
```

### Option 2: Batch Processing (Faster)

```
Single prompt with multiple ontologies:

Convert these v2.0 ontologies to v3.0 format:

1. Organization Ontology:
[PASTE JSON]

2. AI Capability Ontology:
[PASTE JSON]

3. Market Position Ontology:
[PASTE JSON]

[... up to 5 ontologies per batch ...]

For each, generate complete v3.0 with Entry IDs,
glossaries, and test data.

Output: 5 separate v3.0 ontology JSONs
```

---

## 📁 File Organization

### Before Conversion

```
your-project/
├── v2-ontologies/
│   ├── org-ontology-v2/
│   │   ├── ontology.json          ← Need this
│   │   └── glossary.json          ← Optional
│   ├── ai-capability-v2/
│   │   ├── ontology.json          ← Need this
│   │   └── glossary.json          ← Optional
│   └── [... more v2 ontologies ...]
```

### After Conversion

```
your-project/
├── v2-ontologies/              ← Keep for reference
│   └── [original files]
├── v3-ontologies/              ← New converted files
│   ├── org-ontology-v3.json
│   ├── ai-capability-v3.json
│   ├── market-position-v3.json
│   └── [... all v3 ontologies ...]
├── registry.json               ← Catalogue of all
└── baiv-mvp-framework/         ← Final package
    ├── registry.json
    ├── ontologies/
    │   └── [all v3 files]
    └── agents/
```

---

## 🎯 Master Conversion Workflow

### Step-by-Step Process

**1. Inventory v2.0 Files**
```bash
# List all v2.0 ontology JSON files
find . -name "*ontology*.json" -path "*/v2/*"
```

**2. For Each Ontology:**

```
A. Locate files
   ├─ ontology.json ✅ Required
   └─ glossary.json ⚠️ Optional

B. Run conversion prompt
   ├─ Paste ontology JSON
   ├─ (Optional) Mention glossary
   └─ Get v3.0 output

C. Save output
   ├─ Save as [name]-v3.json
   └─ Review for completeness

D. Validate
   ├─ Has @context?
   ├─ Has Entry ID?
   ├─ Has glossary?
   ├─ Has test data?
   └─ Looks complete?

E. Add to registry
   └─ Run registry update prompt
```

**3. After All Conversions:**
```
├─ Validate complete registry
├─ Check all Entry IDs unique
├─ Verify all cross-references
└─ Package for BAIV workflow
```

---

## 📝 Example: Complete Conversion

### Example Ontology: "Organization"

**Input Files You Have:**
```
org-ontology-v2/
├── ontology.json      ← 15 KB
└── glossary.json      ← 8 KB
```

**Step 1: Read ontology.json**
```json
{
  "ontology_metadata": {
    "ontology_name": "Organization Ontology",
    "version": "2.0",
    "domain": "Organization Structure"
  },
  "entities": [
    {
      "entity_name": "Organization",
      "description": "A business entity"
    },
    {
      "entity_name": "Department",
      "description": "A division within org"
    }
  ]
}
```

**Step 2: Run Conversion**
```
Convert this v2.0 ontology to Registry v3.0:

[paste ontology.json contents]

Generate complete v3.0 with Entry ID, glossary, test data.
```

**Step 3: Get v3.0 Output**
```json
{
  "@context": {
    "schema": "https://schema.org/",
    ...
  },
  "@type": "Ontology",
  "@id": "Entry-001",
  "name": "Organization Ontology",
  "version": "3.0",
  "entities": [
    {
      "entityName": "Organization",
      "schemaOrgBase": "Organization",
      ...
    }
  ],
  "glossary": [...],
  "testData": [...],
  "metadata": {...}
}
```

**Step 4: Save**
```bash
# Save as:
v3-ontologies/org-ontology-v3.json
```

**Step 5: Add to Registry**
```
Add this ontology to registry:

[paste registry.json]

New ontology:
[paste org-ontology-v3.json]

Assign Entry-001, update metadata.
```

---

## ❓ FAQ

### Q: Do I need the markdown docs?
**A:** NO. OAA v1.1 regenerates all documentation automatically.

### Q: What about test data files?
**A:** NO. OAA v1.1 generates new test data in v3.0 format.

### Q: Should I include glossary?
**A:** OPTIONAL. If glossary has valuable definitions, paste it for reference. But OAA will regenerate it anyway.

### Q: What if I only have ontology.json?
**A:** PERFECT! That's all you need. OAA v1.1 generates everything else.

### Q: Can I batch convert multiple ontologies?
**A:** YES. Paste 3-5 ontology JSONs in one prompt for batch conversion.

### Q: What if v2.0 structure is messy?
**A:** No problem. OAA v1.1 cleans it up and standardizes to v3.0.

### Q: How do I know conversion worked?
**A:** Check output has:
- @context
- Entry ID
- All entities with schemaOrgBase
- Glossary section
- Test data section
- Metadata complete

---

## 🎯 Quick Reference Card

```
┌─────────────────────────────────────────────────────────────┐
│  CONVERSION QUICK REFERENCE                                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  INPUT FILES NEEDED:                                         │
│  ✅ ontology.json (v2.0)           ← REQUIRED                │
│  ⚠️ glossary.json (v2.0)           ← Optional reference      │
│  ❌ documentation.md               ← Skip, regenerated       │
│  ❌ test-data.json                 ← Skip, regenerated       │
│                                                              │
│  CONVERSION PROCESS:                                         │
│  1. Load ontology.json                                       │
│  2. Paste in conversion prompt                               │
│  3. OAA v1.1 generates v3.0                                  │
│  4. Save as [name]-v3.json                                   │
│  5. Add to registry.json                                     │
│                                                              │
│  OUTPUT YOU GET:                                             │
│  ✅ Complete v3.0 ontology                                   │
│  ✅ NEW glossary (auto-generated)                            │
│  ✅ NEW test data (auto-generated)                           │
│  ✅ NEW docs (auto-generated)                                │
│  ✅ Registry metadata                                        │
│                                                              │
│  TIME PER ONTOLOGY:                                          │
│  Simple:  10 minutes                                         │
│  Complex: 15 minutes                                         │
│  Batch (5): 30 minutes                                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 Conversion Tracking Template

Use this to track your progress:

```
BAIV Ontology Conversion Tracker

[ ] 1. Organization Ontology
    Files: org-ontology-v2/ontology.json
    Status: ___________
    Output: org-ontology-v3.json
    Entry ID: Entry-001

[ ] 2. AI Capability Ontology
    Files: ai-capability-v2/ontology.json
    Status: ___________
    Output: ai-capability-v3.json
    Entry ID: Entry-002

[ ] 3. Market Position Ontology
    Files: market-position-v2/ontology.json
    Status: ___________
    Output: market-position-v3.json
    Entry ID: Entry-003

[ ] 4. Competitive Analysis Ontology
    Files: competitive-analysis-v2/ontology.json
    Status: ___________
    Output: competitive-analysis-v3.json
    Entry ID: Entry-004

[ ] 5. AI Maturity Ontology
    Files: ai-maturity-v2/ontology.json
    Status: ___________
    Output: ai-maturity-v3.json
    Entry ID: Entry-005

[ ] 6. Visibility Metrics Ontology
    Files: visibility-metrics-v2/ontology.json
    Status: ___________
    Output: visibility-metrics-v3.json
    Entry ID: Entry-006

[ ] 7. Strategy Recommendation Ontology
    Files: strategy-recommendation-v2/ontology.json
    Status: ___________
    Output: strategy-recommendation-v3.json
    Entry ID: Entry-007

[ ] 8. Action Item Ontology
    Files: action-item-v2/ontology.json
    Status: ___________
    Output: action-item-v3.json
    Entry ID: Entry-008

[ ] 9. Timeline Ontology
    Files: timeline-v2/ontology.json
    Status: ___________
    Output: timeline-v3.json
    Entry ID: Entry-009

[ ] 10. Deliverable Ontology
    Files: deliverable-v2/ontology.json
    Status: ___________
    Output: deliverable-v3.json
    Entry ID: Entry-010

[ ] NEW: Business Process Ontology (create new)
    Files: N/A - create from scratch
    Status: ___________
    Output: business-process-baiv-v3.json
    Entry ID: Entry-011

Progress: __ / 11 complete
```

---

## ✅ Summary

### What You Need for Conversion

```
PER ONTOLOGY:
✅ ontology.json file (v2.0)       ← THIS IS ALL YOU NEED!

OPTIONAL:
⚠️ glossary.json (for reference)

NOT NEEDED:
❌ Markdown documentation
❌ Test data files
❌ Any other supporting files
```

### Conversion Outputs

```
PER ONTOLOGY:
✅ Complete v3.0 ontology JSON
    ├─ @context and Entry ID
    ├─ All entities with schema.org
    ├─ Relationships and rules
    ├─ NEW glossary (auto-generated)
    ├─ NEW test data (auto-generated)
    └─ Complete metadata
```

### Time Estimate

```
10 ontologies × 15 min = 2.5 hours
Registry updates         = 30 min
Total conversion time    = 3 hours
```

---

**The key insight: You only need the v2.0 ontology JSON file. Everything else is regenerated by OAA v1.1!**

**Version:** 1.0  
**Date:** 2025-10-13  
**Status:** ✅ Complete Catalogue

